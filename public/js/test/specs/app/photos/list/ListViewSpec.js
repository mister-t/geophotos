// Jasmine Unit Testing Suite
define(['jquery', 'backbone', 'marionette'], function($, Backbone, Marionette) {
  // Test suite that includes all unit tests
  describe('Photos List', function() {
    it('should display a list of photos', function () {
    });

    it('should have a list of cities to choose from', function () {
    });

    it('should have a list of animation effects', function () {
    });

    describe('a photo in the list', function () {
      it('should image source', function () {
      });

      it('should have a caption that is hidden', function () {
      });

      it('should a photo ID', function () {
      });

      describe('when the mouse pointer is hovering over the photo', function () {
        it('should display the caption', function () {
        });

        it('should enlarge the photo from its original state', function () {
        });
      });
    });
  });
});
